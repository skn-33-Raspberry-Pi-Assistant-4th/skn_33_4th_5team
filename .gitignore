# ══════════════════════════════════════════════════════════════
# 🔴 자료 파일 — 이 저장소는 공개다 (D-29)
#
# 코퍼스에 이용약관 제약이 있는 자료가 섞여 있어 원문을 커밋하지 않는다.
# data/manifests/ 의 대장 4종만 올리고, 자료는 팀 내부 아카이브로 공유한다.
# 재현 절차는 data/README.md 참조.
# ══════════════════════════════════════════════════════════════
command_md/*
data/*
!data/.gitkeep
!data/README.md
!data/manifests/
!data/manifests/**
# Corpus 카드에는 원문 본문을 넣지 않고 fixture의 용도와 사용 금지를 기록한다.
# 팀원이 legacy corpus를 서비스 입력으로 오인하지 않도록 이 문서만 추적한다.
!data/corpora/
data/corpora/*
!data/corpora/*/
data/corpora/*/*
!data/corpora/*/corpus_card.md

# 하위 디렉터리 자체는 남긴다 — data/* 가 디렉터리를 배제하면
# git 이 그 안으로 내려가지 않아 .gitkeep 이 통째로 무시된다.
!data/raw/
!data/snapshot/
!data/extracted/
!data/structured/
!data/indexed/
!data/train/
data/raw/*
data/snapshot/*
data/extracted/*
data/structured/*
data/indexed/*
data/train/*
!data/*/.gitkeep
!data/*/README.md

# 사실 표는 **우리 산출물**이라 커밋한다 (D-37).
# 원문이 아니라 사실이고, 스키마·분류·문장화는 우리가 만든 것이다.
# 4명이 나눠 쓴 것을 병합하려면 저장소를 거쳐야 하고, 재현성에도 필요하다.
#
# ⚠ 디렉터리 자체를 먼저 살려야 한다 — 위 raw/snapshot 과 같은 함정이다.
#   `data/*` 가 data/facts/ 를 배제하면 git 이 그 안으로 내려가지 않아
#   `!data/facts/*.csv` 가 **한 줄도 효력이 없다.**
#   2026-08-01 병합 준비 중 발견 — 888행 사실 표가 통째로 커밋되지 않고 있었다.
!data/facts/
!data/facts/*.csv
!data/facts/*.md

# 제품 catalog는 원문 corpus가 아닌 팀이 검수한 실행용 사실 데이터다.
# 제품 추천 서비스가 clone 직후 동일한 후보·근거 ID를 사용해야 하므로 커밋한다.
# `data/*`의 영향을 해제하려면 디렉터리와 파일을 모두 예외 처리해야 한다.
!data/products/
!data/products/*.json
!data/products/README.md

# 사용자용 표시 라벨은 원문 corpus가 아닌 코드가 관리하는 UI 사전이다.
!data/presentation/
!data/presentation/citation_labels_ko.json

# 문서·표 형식 자료 (매니페스트·규칙표·골든셋만 예외)
*.pdf
# ⚠️ **산출물 PDF 는 예외다.** README 맨 위가 이 파일들을 링크하는데
#   `*.pdf` 에 걸려 저장소에 없으면 **첫 화면의 링크가 404 가 된다** (2026-08-04 발견).
#   `git add -f` 로 밀어 넣지 않는다 — 그러면 다음 사람이 왜 들어갔는지 모른다.
!docs/*.pdf
!docs/그림/*.pdf
!docs/발표자료/*.pdf
!eval/reports/결과보고서_제출용/*.pdf
*.csv
*.xlsx
*.zip
!data/manifests/*.csv
!data/facts/*.csv
!src/pettriage/compute/tables/*.csv
!eval/goldenset/*.csv

# ══════════════════════════════════════════════════════════════
# 학습 산출물 (D-42)
#
# ⚠️ gitignore 에서 `#` 은 **행 첫 글자일 때만** 주석이다.
#    패턴 뒤에 주석을 붙이면 패턴 전체가 리터럴이 되어 아무것도 매치하지 않는다.
#    그래서 설명은 반드시 패턴 위 줄에 쓴다.
# ══════════════════════════════════════════════════════════════
# 루트 한정 — src/pettriage/models/ 는 소스다
/models/
/checkpoints/
/adapters/
/outputs/
# 실행 시 코드가 만든다 (qlora.py). 빈 폴더를 저장소에 둘 이유가 없다.
# 커밋 메시지 임시 파일.
#
# cmd 는 여러 줄 문자열을 못 받아 `git commit -F` 로 파일을 넘기게 되는데,
# 커밋 **전에** `git add -A` 가 이것을 잡아간다. 실제로 한 번 딸려 들어갔다 (2026-08-02).
# 더 확실한 방법은 저장소 밖에 두는 것이다 — `git commit -F %TEMP%\commit_msg.txt`
commit_msg.txt
COMMIT_MSG*

/artifacts/
*.safetensors
*.bin
*.gguf
*.pt
*.ckpt

# ── 벡터DB · 캐시 ─────────────────────────────────────────
.chroma/
*.sqlite3
.cache/

# ── 비밀 ──────────────────────────────────────────────────
.env
.env.*
.env_copy
!.env.example
*.pem
*.key

# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
web_app/staticfiles/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ── 노트북 ────────────────────────────────────────────────
.ipynb_checkpoints/

# ── 편집기 · OS ───────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ══════════════════════════════════════════════════════════════
# 검토 · 병합 잔여물
#
# `lgj_review.diff`(54KB)가 실제로 커밋 직전까지 갔다 (2026-08-02).
# 팀원 코드를 흡수할 때(D-48) 매번 생기는 파일들이고, 저장소에 남을 이유가 없다.
# ══════════════════════════════════════════════════════════════
*.diff
*.patch
*.orig
*.rej

# ══════════════════════════════════════════════════════════════
# 평가 결과
#
# **조건·날짜가 붙은 리포트는 커밋한다** — 산출물 ④의 원자료다.
# 이름 규칙은 eval/reports/README.md 에 있다:
#     2026-08-02_기준선_모델없음.json
#     2026-08-03_A_대형LLM.json
#
# 덮어쓰는 latest 만 뺀다 — `make eval` 이 매 실행마다 같은 파일을 갈아쳐서
# 41KB 가 통째로 diff 에 뜬다. **무엇이 바뀌었는지 안 보이는 diff 는 없느니만 못하다.**
# ══════════════════════════════════════════════════════════════
eval/reports/latest.json

# ── 로그 · OS · 도구 ──────────────────────────────────────
*.log
desktop.ini
*:Zone.Identifier
.hypothesis/
node_modules/

# - 임시 폴더입니다. 잠깐 데이터 보관할 폴더여서 제외로 추가합니다. lgj
temp/

# ── 문서 파이프라인 ─────────────────────────────────────
# 원문·정제본·생성 manifest는 재생성 가능한 데이터이므로 커밋하지 않는다.
# 계약, 출처 대장, 라이선스 검토 기록만 저장소에서 공동 검수한다.
document_pipeline/data/raw/*
!document_pipeline/data/raw/README.md
document_pipeline/data/processed/*
!document_pipeline/data/processed/README.md
document_pipeline/data/manifest.json
document_pipeline/data/media_chunk_map*.json
!document_pipeline/data/source_registry.csv
document_pipeline/data/raw_v*/
document_pipeline/data/processed_v*/
document_pipeline/data/manifest_v*.json
document_pipeline/data/media_manifest.json
document_pipeline/data/media_manifest_v*.json
!document_pipeline/data/source_registry_v*.csv

# ══════════════════════════════════════════════════════════════
# 제출 묶음 (생성물)
#
# `make submit` 이 만드는 `SKN_3rd_5Team_REPORT/` 는 **전부 사본**이다.
# 원본은 docs/ · data/ · eval/ 에 있고, 커밋하면 같은 내용이 저장소에 두 번
# 들어간다. 한쪽만 고쳐지는 순간 저장소가 거짓말을 시작한다 (D-22 · 단일 출처).
#
# 제출은 `make submit-zip` 이 만든 묶음을 올린다. 저장소로 제출하는 경우에는
# 루트 README.md 의 「필수 산출물 넷」 표가 같은 대조표 역할을 한다.
# ══════════════════════════════════════════════════════════════
SKN_3rd_5Team_REPORT/
SKN_3rd_5Team_REPORT_*.zip


*.ses

# 공식 문서 전체 사본은 저장소에 두지 않는다.
# corpus에 실제로 쓰이는 원문은 파이프라인이 고정 commit에서 받아
# document_pipeline/data/raw_v3/ 에 생성한다.
# 전체 원문을 오프라인으로 볼 필요가 있을 때만 README의 안내대로 내려받는다.
/document/
/document
