# GPU 추론 환경 (RunPod CUDA Pod)
#
#   pip install -r requirements-gpu.txt
#
# 기본 환경을 포함하므로 requirements.txt를 따로 설치하지 않아도 된다.
# Qwen3-4B 답변 생성과 LoRA 조건 추출을 4-bit로 실행할 때 필요하다.
# torch는 RunPod CUDA 이미지에 포함된 호환 빌드를 사용하므로 여기서 재설치하지 않는다.

-r requirements.txt

accelerate>=1.6,<2
# transformers의 4-bit(NF4) 양자화 경로가 요구한다.
bitsandbytes>=0.46,<1
# 학습된 LoRA adapter를 Base 모델에 얹어 조건 추출에 사용한다.
peft>=0.17,<1
huggingface_hub>=0.34,<2
